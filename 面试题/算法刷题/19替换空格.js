/**
 * @param {string} s
 * @return {string}
 */
var replaceSpace = function(s) {
    // return s.replace(/ /g,"%20")
    // return s.split(' ').join('%20')
};

var s = "We are happy."

console.log(replaceSpace(s));
